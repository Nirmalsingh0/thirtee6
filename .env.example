DATABASE_URL="file:./dev.db"

# Admin credentials for first login (seed uses these defaults if unset)
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="admin12345"

# Cookie/JWT secret (set a long random string in production)
AUTH_SECRET="change-me-in-production"

